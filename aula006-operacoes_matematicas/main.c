#include <stdio.h>

int main()
{
  int x = 10;
  int y = 2;
  int soma = x + y;
  int sub = x - y;
  int mult = x * y;
  int div = x / y;
  printf("%i\n", soma);
  printf("%i\n", sub);
  printf("%i\n", mult);
  printf("%i\n", div);
  return 0;
}
